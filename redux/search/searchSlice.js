// import {createSlice} from "@reduxjs/toolkit";

// const defaultState = {
//   searchItems: [],
// };

// export const SearchReducer = createSlice({
//   name: "search",
//   initialState: defaultState,
//   reducers: {
//     itemsFiltered: (state, action) => {
//       state.searchItems = state.searchItems.filter((searchItem) =>
//         searchItem.title.toLowerCase().includes(action.payload)
//       );
//     },
//   },
// });

// export const {itemsFiltered} = SearchReducer.actions;
// export default SearchReducer.reducer;
